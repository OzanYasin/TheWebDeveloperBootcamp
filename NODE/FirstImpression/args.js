console.log('HELLO FROM ARGS FILE!');
console.log(process.argv); // path of that js file

// Any remaining elements will be additional command line arguments so I can pass in arguments to a script, not to a function.
// It's a similar idea I can pass in arguments to a script.
